{
  "name": "advocate-website",
  "version": "1.0.0",
  "description": "Professional website for Advocate Anil Kumar Singh",
  "scripts": {
    "start": "cd server && npm start",
    "dev": "concurrently \"cd client && npm start\" \"cd server && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "install:all": "npm run install:client && npm run install:server",
    "render-postbuild": "npm run install:all && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/asaditya25/Advocate-website.git"
  },
  "author": "Aditya Singh",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
